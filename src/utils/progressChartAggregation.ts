
// Define the minimum number of dates required to render the chart 
const MIN_UNIQUE_DATES = 5;

// Series [{}, {}]

type CalcProps = {
    dateList: string[];
};

export function calcUniqueSubDates({ dateList }: CalcProps) {

    const dateConvert = new Date()

}

export function determineAggregationLevel() {

}

type AggProgressProps = {
    series: string[];
    aggLevel: number;
};

export function aggregateProgressSeries({ series, aggLevel }: AggProgressProps) {

}
